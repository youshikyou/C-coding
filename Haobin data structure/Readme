链表特征:
链表的数据结构是由两部分组成，第一部分是data，第二部分是下一个node的地址。
链表是有一个head node，它无有效数据，但存放第一个node的地址。
由head node可以遍寻整个链表。

head node
first node:first valid node
last node:last valid node

动态栈特征:
动态栈本身是一个两个指针组成的数据结构， 这个结构里面有指向栈底的指针和指向栈顶的指针。
栈底指针永远指向head node，栈顶指针指向最后一个node。
node本身存储的数据可以自由定义类型。

静态队列特征：
静态队列的数据结构是由指向数组的指针 pBase，数组第一个元素位置front index和和数组最后一个元素位置的后一个位置的rear index 三部分组成。
当front index = rear index时，队列为空。
判断队列满的时候，不能单纯的利用front和rear的相等来判断，因为他们有可能是空队列
  两个办法去解决
  1). 只允许队列最多存LEN—1个元素。（rear+1）%LEN ==f 就是满了。
  2).加一个变量来记录元素多少，不常用！

递归：
1.递归必须要有一个明确的终止条件
2.该函数处理的数据规模必须递减
3.这个转化必须是可解的


